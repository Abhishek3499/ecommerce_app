import 'package:flutter/material.dart';

class OtpBackground extends StatelessWidget {
  const OtpBackground({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      "assets/password_screen/Bubbles (3).png",
      fit: BoxFit.cover,
    );
  }
}
